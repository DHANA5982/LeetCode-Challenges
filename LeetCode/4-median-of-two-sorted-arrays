nums1 = [1, 2 , 4]
nums2 = [1,3]

def median_1(nums1, nums2):
    a = sorted(nums1+nums2)
    if len(a)%2 == 0:
        b = a[int(len(a)/2)] + a[int(len(a)/2)-1]
        print(b/2)
    else:
        print(a[int(len(a)/2)])


median_1(nums1, nums2)